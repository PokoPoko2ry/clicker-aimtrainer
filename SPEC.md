# AIM × CLICKER 3D 仕様書

3Dエイムトレーナーとクリッカー/放置ゲームを融合させた、単一HTMLで動くブラウザゲーム。
的を撃ってスコアとポイントを稼ぎ、ポイントで永続強化しながらエンドレスなウェーブを
攻略していく。

---

## 1. 技術構成

| 項目 | 内容 |
|---|---|
| 形態 | 単一ファイル `index.html`（HTML + CSS + ESM JavaScript） |
| 3Dエンジン | [Three.js](https://threejs.org/) 0.160.0（importmap経由でCDN読み込み） |
| 描画 | WebGLRenderer（pixelRatio上限2、アンチエイリアス有効） |
| 音 | Web Audio API（OscillatorによるSE生成、外部音源なし） |
| 保存 | localStorage |
| 対応 | PC（マウス＋ポインターロック）／タッチ端末（仮想スティック＋ボタン） |
| 依存 | ネット接続（Three.js CDN読み込みのため）。失敗時はエラーメッセージ表示 |

---

## 2. 画面構成とナビゲーション

`.screen` オーバーレイの表示/非表示で画面遷移する（同時に1画面のみ表示）。

```
ロビー ─┬─ 射撃場（モード選択）─┬─ フリック → プレイ
        │                        └─ トラッキング → プレイ
        ├─ ショップ（永続強化）
        └─ 設定（感度・Y反転・ゲーム感度合わせ）
```

- **ロビー**: 所持ポイント／最高ウェーブ／現在到達ゾーンを表示。放置報酬があれば通知。
- **射撃場**: フリック／トラッキングの2モードから選択。
- **ショップ**: ポイントで10種のアップグレードを購入。
- **設定**: マウス/スティック感度、Y軸反転、実ゲーム感度からの換算。
- **プレイ中**: HUD（Score / Quota / Combo / Accuracy / Wave / Points）を表示。

---

## 3. ゲームモード

### 3.1 フリック（Flick）
- 画面中央のレティクルへ的を「振って」合わせ、**単発**で撃ち抜く。
- **弾倉（マガジン）**制。弾切れで自動リロード、手動リロードも可能。
- ウェーブごとに「討伐ノルマ」を満たすと次ウェーブへ。

### 3.2 トラッキング（Tracking）
- 常時移動する1体の的に、照準を**乗せ続ける**。
- マウス/タップを押している間、かつ的に乗っている間だけ加点。
- ノルマは「照準を乗せ続けた累積秒数」。

---

## 4. コアループ（クリッカー進行）

### 4.1 エンドレスウェーブ
- 固定時間制ではなく、**ノルマ達成→次ウェーブ**を無限に繰り返す。
- 難易度はウェーブ数で上昇：`difficulty = (wave − 1) × 3.5`

| パラメータ | 計算式 |
|---|---|
| ノルマ（フリック） | `6 + floor((wave − 1) × 1.4)` 体 |
| ノルマ（トラッキング） | `5 + (wave − 1) × 1.2` 秒 |
| 出現間隔 | `max(0.30, 0.95 − difficulty × 0.012)` 秒 |
| 的の半径 | `0.6 × max(0.5, 1 − difficulty × 0.006) × サイズ補正` |
| 的の滞在時間 | `2.4 × max(0.55, 1 − difficulty × 0.005) × 滞在補正` |
| 同時出現上限 | `round(同時出現数)` |

### 4.2 ボスウェーブ
- フリックモードで**5ウェーブごと**（wave % 5 == 0）に発生。
- HP制の大型ボスが1体出現。通常弾を当てるたびにHPを削り、0で撃破。
  - ボスHP = `18 + wave × 4`
  - 撃破報酬 = 通常撃破の**12倍**
- ボス戦中は通常の的は出現しない。撃破すると次ウェーブへ。
- ボスはHP残量に応じて縮小し、発光で存在を主張する。

### 4.3 ゾーン（環境解放）
到達ウェーブに応じて環境が解放され、部屋と的の配色が変化。**全体倍率**がスコア/ポイントに乗る。

| 解放ウェーブ | ゾーン名 | 全体倍率 |
|---|---|---|
| 1 | 射撃場 | ×1 |
| 10 | ネオン都市 | ×2 |
| 25 | クライオ基地 | ×4 |
| 50 | 軌道ステーション | ×8 |
| 100 | 特異点 | ×16 |

---

## 5. スコア／ポイント／コンボ

- **Score**: セッション内の一時的な成績（プレイ開始で0にリセット）。
- **Points**: 永続通貨。ショップ強化に使用（引き継ぎ・保存対象）。

### 5.1 フリック撃破
```
comboMult = 1 + min(combo, 50) × 0.05
goldMult  = 黄金の的 ? 5 : 1
bossMult  = ボス ? 12 : 1
gained(Score) = round(10 × 得点倍率 × comboMult × goldMult × bossMult × ゾーン倍率)
pts(Points)   = round(gained × 0.5 × ポイント効率)
```
- **黄金の的**: 6%の確率で出現。撃破で5倍＆ボーナス演出、滞在時間1.2倍。
- **コンボ**: 撃破ごとに+1。ミス（撃ち外し／的の消滅）で `combo = floor(combo × コンボ保護)` に減衰。

### 5.2 トラッキング加点
```
comboMult = 1 + min(streak秒, 12) × 0.08
gain(Score/秒) = 75 × 得点倍率 × comboMult × ゾーン倍率
pts/秒         = gain × 0.5 × ポイント効率
```
- 照準を外すと streak（連続維持秒）は0にリセット。

---

## 6. アップグレード（ショップ）

コスト = `floor(base × growth^現在Lv)`。効果は下表の通り。

| ID | 名称 | base | growth | 最大Lv | 効果（Lv=l） |
|---|---|---|---|---|---|
| mult | 得点倍率 | 20 | 1.55 | 40 | `1 + l × 0.25` |
| size | 的のデカさ補正 | 30 | 1.60 | 20 | `1 + l × 0.06` |
| life | 的の滞在時間 | 35 | 1.60 | 18 | `1 + l × 0.10` |
| mag | 弾倉容量（フリック） | 40 | 1.55 | 12 | `12 + l × 2` 発 |
| reload | リロード速度（フリック） | 45 | 1.60 | 12 | `1 / (1 + l × 0.18)` 倍の時間 |
| spawn | 同時出現数 | 60 | 1.75 | 8 | `3 + l` 体 |
| combo | コンボ保護 | 80 | 1.90 | 8 | ミス時 `min(0.8, l × 0.1)` を残す |
| bot | オートボット | 120 | 1.80 | 12 | `l` 体（自動撃破／放置収益） |
| botspd | ボット連射力 | 100 | 1.70 | 15 | `1 / (1 + l × 0.18)` 倍の間隔 |
| gold | ポイント効率 | 50 | 1.65 | 30 | `1 + l × 0.2` |

### 6.1 リロード（フリック）
- 基本リロード時間 `BASE_RELOAD = 1.8` 秒 → 実時間 `1.8 × リロード速度`。
- 弾倉最大 = `round(弾倉容量)`。弾切れ時は自動、`R`キー/リロードボタンで手動。

### 6.2 オートボット
- 発射間隔 = `(1.1 × ボット連射力) / max(1, ボット数)` 秒ごとに作動。
- フリック: 最も古い的を自動撃破（ボスにはダメージ）。
- トラッキング: `round(12 × 得点倍率)` を自動加点。

---

## 7. 永続化（セーブ）

localStorage キー `aim_save` に JSON 保存。

```json
{ "v": 1, "points": <所持ポイント>, "best": <最高ウェーブ>,
  "up": { "<upgradeId>": <Lv>, ... }, "seen": <最終保存時刻(ms)> }
```

- 保存タイミング: 強化購入時 / ウェーブ更新時 / ロビー帰還時 / タブを閉じる・隠す時。
- 読込タイミング: 起動時（強化Lv・ポイント・最高ウェーブを復元）。
- 別キー: `aim_sens`（感度倍率）, `aim_invert`（Y反転）, `aim_mode`（最後のモード）。

### 7.1 放置（オフライン）報酬
- **オートボット所持時のみ**発生する補助収益（アクティブプレイが主役の設計）。
- 計算（上限2時間、最短60秒から）:
  ```
  secs    = min(経過秒, 7200)
  perKill = round(10 × 得点倍率 × 0.5 × ポイント効率)
  rate    = ボット数 / 1.1        // ざっくり毎秒討伐数
  gain    = floor(perKill × rate × secs × 0.2)   // オフラインは×0.2に抑制
  ```
- 起動時にポイントへ加算し、ロビーに「おかえり！ +N pts」通知を表示。

---

## 8. 操作

### 8.1 PC
| 操作 | 動作 |
|---|---|
| マウス移動 | 視点移動（ポインターロック中） |
| 左クリック | フリック=発射／トラッキング=押している間だけ加点 |
| `R` キー | プレイ中=リロード／TIME UP中=リスタート |
| ロック解除（Esc） | 一時停止 |

- 視点は水平 `±0.7 rad`・垂直 `±0.55 rad` に制限。
- ポインターロック取り直し直後の巨大デルタ、境界スパイク（>180px）は無視。

### 8.2 タッチ
- 左下: 視点スティック（倒し量で視点速度）
- 右下: FIRE ボタン（発射／トラッキング保持）
- リロードボタン（フリック時のみ）／一時停止ボタン
- ポインターロックが無いため擬似ロックでプレイ開始。

### 8.3 感度
- 内部基準 `BASE_SENS = 0.0022 rad/px`、倍率 `0.1〜3.0`。
- **ゲーム感度合わせ**: 対象ゲーム係数 × ゲーム内sens から内部倍率を算出。
  - Apex Legends: 係数 `0.022` ／ Overwatch 2: 係数 `0.0066`
  - 前提: 同一マウスDPI＋OSマウス加速オフ。

---

## 9. 演出・サウンド

- **ポップアップ**: 撃破位置に獲得スコアを表示。
- **コンボフラッシュ**: 10コンボごと／ボーナス／ウェーブ更新／ゾーン解放/ボス出現。
- **バースト**: 撃破位置にリング＋フラッシュのパーティクル。
- **SE**: 命中・ミス・購入・ボーナス・弾切れ・リロード開始/完了をOscillatorで生成。

---

## 10. 状態遷移（プレイ）

```
起動 → loadGame（復元＋放置報酬）→ ロビー
ロビー → startGame → プレイ（running）
プレイ ⇄ 一時停止（ポインターロック解除 / ポーズボタン）
プレイ → ロビー帰還（saveGame）
```

- 現行は時間切れによる自動終了は無く、ウェーブは離脱するまで無限に継続。
  最高ウェーブ（best）がメタ進行の指標として保存される。

---

## 11. 今後の拡張候補（未実装）

- **プレステージ**: 進行をリセットして恒久メタ通貨（Focus）を獲得する第二ループ。
- **ゾーン別ギミック的**: 分裂・シールド・テレポートなど挙動の異なる的。
- **新アップグレード**: ボスダメージ、ゾーン倍率強化、黄金出現率など。
- **トラッキングのボス演出**、実績/チャレンジ、リーダーボード。
